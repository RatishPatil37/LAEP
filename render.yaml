services:
  - type: web
    name: laep-api
    env: python
    buildCommand: pip install -r laep-api/requirements.txt
    startCommand: cd laep-api && uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
